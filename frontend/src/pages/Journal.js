import { useState, useEffect } from "react";
import axios from "axios";
export default function Journal() { const [entries, setEntries] = useState([]); const [title, setTitle] = useState(""); const [content, setContent] = useState(""); const token = localStorage.getItem("token"); const headers = { Authorization: "Bearer " + token }; const fetchEntries = async () => { const res = await axios.get("http://localhost:5001/api/journal", { headers }); setEntries(res.data); }; useEffect(() => { fetchEntries(); }, []); const handleSubmit = async (e) => { e.preventDefault(); await axios.post("http://localhost:5001/api/journal", { title, content }, { headers }); setTitle(""); setContent(""); fetchEntries(); }; const handleDelete = async (id) => { await axios.delete("http://localhost:5001/api/journal/" + id, { headers }); fetchEntries(); }; return (<div style={{ maxWidth: "600px", margin: "40px auto", padding: "30px" }}><h2>Journal</h2><form onSubmit={handleSubmit}><input type="text" placeholder="Title" value={title} onChange={e => setTitle(e.target.value)} required style={{ width: "100%", padding: "10px", marginBottom: "10px", borderRadius: "5px", border: "1px solid #ccc" }} /><textarea placeholder="Write your thoughts..." value={content} onChange={e => setContent(e.target.value)} required rows="5" style={{ width: "100%", padding: "10px", marginBottom: "10px", borderRadius: "5px", border: "1px solid #ccc" }} /><button type="submit" style={{ padding: "10px 25px", background: "#4a90d9", color: "white", border: "none", borderRadius: "5px", cursor: "pointer" }}>Save Entry</button></form><div style={{ marginTop: "30px" }}>{entries.map(e => (<div key={e._id} style={{ padding: "15px", marginBottom: "10px", border: "1px solid #eee", borderRadius: "8px" }}><h3 style={{ margin: "0 0 5px" }}>{e.title}</h3><p style={{ color: "#666", margin: "0 0 5px" }}>{new Date(e.createdAt).toLocaleDateString()}</p><p>{e.content}</p><button onClick={() => handleDelete(e._id)} style={{ color: "red", background: "none", border: "none", cursor: "pointer" }}>Delete</button></div>))}</div></div>); }
