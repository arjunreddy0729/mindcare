import { useState, useEffect } from "react";
import axios from "axios";
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from "recharts";
export default function MoodHistory() { const [moods, setMoods] = useState([]); useEffect(() => { const fetchMoods = async () => { const token = localStorage.getItem("token"); const res = await axios.get("http://localhost:5001/api/mood", { headers: { Authorization: "Bearer " + token } }); setMoods(res.data.reverse()); }; fetchMoods(); }, []); const chartData = moods.map(m => ({ date: new Date(m.date).toLocaleDateString(), rating: m.rating, mood: m.mood })); return (<div style={{ maxWidth: "700px", margin: "40px auto", padding: "30px" }}><h2>Mood History</h2>{chartData.length > 0 ? (<ResponsiveContainer width="100%" height={300}><LineChart data={chartData}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="date" /><YAxis domain={[1, 5]} /><Tooltip /><Line type="monotone" dataKey="rating" stroke="#4a90d9" strokeWidth={2} /></LineChart></ResponsiveContainer>) : <p>No mood data yet.</p>}<div style={{ marginTop: "20px" }}>{moods.map(m => (<div key={m._id} style={{ padding: "10px", borderBottom: "1px solid #eee" }}><strong>{m.mood}</strong> — {m.rating}/5 — {new Date(m.date).toLocaleDateString()}{m.note && <p style={{ color: "#666", margin: "5px 0 0" }}>{m.note}</p>}</div>))}</div></div>); }
