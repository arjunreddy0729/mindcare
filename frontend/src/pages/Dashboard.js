import { useState, useEffect } from "react";
import axios from "axios";
export default function Dashboard() { const [moods, setMoods] = useState([]); const [journals, setJournals] = useState([]); useEffect(() => { const token = localStorage.getItem("token"); const headers = { Authorization: "Bearer " + token }; axios.get("http://localhost:5001/api/mood", { headers }).then(res => setMoods(res.data)); axios.get("http://localhost:5001/api/journal", { headers }).then(res => setJournals(res.data)); }, []); const latest = moods[0]; const avgRating = moods.length ? (moods.reduce((a, m) => a + m.rating, 0) / moods.length).toFixed(1) : 0; return (<div style={{ maxWidth: "600px", margin: "40px auto", padding: "30px" }}><h2>Welcome to MindCare</h2><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "15px", marginTop: "20px" }}><div style={{ padding: "20px", background: "#e8f4fd", borderRadius: "10px", textAlign: "center" }}><h3>Total Moods Logged</h3><p style={{ fontSize: "30px", fontWeight: "bold", color: "#4a90d9" }}>{moods.length}</p></div><div style={{ padding: "20px", background: "#e8f8e8", borderRadius: "10px", textAlign: "center" }}><h3>Avg Rating</h3><p style={{ fontSize: "30px", fontWeight: "bold", color: "#4a90d9" }}>{avgRating}/5</p></div><div style={{ padding: "20px", background: "#fff3e0", borderRadius: "10px", textAlign: "center" }}><h3>Journal Entries</h3><p style={{ fontSize: "30px", fontWeight: "bold", color: "#4a90d9" }}>{journals.length}</p></div><div style={{ padding: "20px", background: "#f3e5f5", borderRadius: "10px", textAlign: "center" }}><h3>Latest Mood</h3><p style={{ fontSize: "30px", fontWeight: "bold", color: "#4a90d9" }}>{latest ? latest.mood : "N/A"}</p></div></div></div>); }
