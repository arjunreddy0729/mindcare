import { useState, useEffect } from "react";
import axios from "axios";
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, BarChart, Bar } from "recharts";
export default function WeeklyReport() { const [report, setReport] = useState(null); useEffect(() => { const fetchReport = async () => { const token = localStorage.getItem("token"); const res = await axios.get("http://localhost:5001/api/report", { headers: { Authorization: "Bearer " + token } }); setReport(res.data); }; fetchReport(); }, []); if (!report) return <p style={{ textAlign: "center", marginTop: "40px" }}>Loading...</p>; const moodData = report.moods.map(m => ({ date: new Date(m.date).toLocaleDateString(), rating: m.rating })); const stressData = report.stresses.map(s => ({ date: new Date(s.date).toLocaleDateString(), level: s.level })); return (<div style={{ maxWidth: "700px", margin: "40px auto", padding: "30px" }}><h2>Weekly Wellness Report</h2><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: "15px", marginBottom: "30px" }}><div style={{ padding: "20px", background: "#e8f4fd", borderRadius: "10px", textAlign: "center" }}><h4>Avg Mood</h4><p style={{ fontSize: "28px", fontWeight: "bold", color: "#4a90d9" }}>{report.avgMood}/5</p></div><div style={{ padding: "20px", background: "#fde8e8", borderRadius: "10px", textAlign: "center" }}><h4>Avg Stress</h4><p style={{ fontSize: "28px", fontWeight: "bold", color: "#e74c3c" }}>{report.avgStress}/10</p></div><div style={{ padding: "20px", background: "#e8f8e8", borderRadius: "10px", textAlign: "center" }}><h4>Goals Done</h4><p style={{ fontSize: "28px", fontWeight: "bold", color: "#4CAF50" }}>{report.goalsCompleted}/{report.totalGoals}</p></div></div>{moodData.length > 0 && (<div style={{ marginBottom: "30px" }}><h3>Mood Trend</h3><ResponsiveContainer width="100%" height={200}><LineChart data={moodData}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="date" /><YAxis domain={[1, 5]} /><Tooltip /><Line type="monotone" dataKey="rating" stroke="#4a90d9" strokeWidth={2} /></LineChart></ResponsiveContainer></div>)}{stressData.length > 0 && (<div><h3>Stress Trend</h3><ResponsiveContainer width="100%" height={200}><BarChart data={stressData}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="date" /><YAxis domain={[1, 10]} /><Tooltip /><Bar dataKey="level" fill="#e74c3c" /></BarChart></ResponsiveContainer></div>)}<div style={{ marginTop: "20px", padding: "15px", background: "#f9f9f9", borderRadius: "10px" }}><p>Moods logged: {report.totalMoods} | Stress logged: {report.totalStress} | Journal entries: {report.totalJournals}</p></div></div>); }
